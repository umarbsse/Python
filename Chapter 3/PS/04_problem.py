#check and replace double space
name="Umer is a good boy  and  "
print(name.replace("  "," "))  # temp string change not original one
print(name) # string are immutable which mean you can not change them by excuting the functions on them