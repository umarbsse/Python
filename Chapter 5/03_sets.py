#set and dictionary both use same curly brackets

s = {}  # like the empty Dictionary
print(type(s))


empty_set = set() # It creates an empty set

print(type(empty_set))

set_1 = {1,2,3,4,5,6,67,7,8,8,1,1,1,1,1,1}   # set does not get reptative elements , Its auto rejects the sets

# order does not maintain in sets, if want to maintain order get use list instead of set

print(set_1)